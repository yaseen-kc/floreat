/**
 * Rate schema — thin re-export of the canonical contract from
 * `@floreat/shared/schemas`. Controllers/services import from here so the
 * `.js`-specifier relative import stays stable (see backend standards §8).
 */
export * from '@floreat/shared/schemas'
