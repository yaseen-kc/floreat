/**
 * Canopy request contracts — re-exported from the single source of truth in
 * `@floreat/shared/schemas`. Kept as a thin shim so existing
 * `../schemas/canopy.schema.js` imports (and `paginationSchema`) keep working.
 */
export * from '@floreat/shared/schemas'
